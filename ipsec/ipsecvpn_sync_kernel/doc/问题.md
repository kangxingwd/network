
# gcc 编译后总是链接到 libnl-3.so.200
- 就是链接到 libnl-3.so.200， libnl 编译完成安装后 lib 下就是有 libnl-3.so.200， 也需要拷贝过去。或者建立软连接


# -lnl-xfrm-3 链接不上这个库， 编译出来的二进制 ldd 后没有链接这个库

- 有时候 -l 链接了某个库，但是ldd发现没有链接进去， 是因为gcc ld 做了些优化，没有实际用的库不会被链接进去（可能会有深层次的调用没有探测到）， 可以使用  -Wl,--no-as-needed   添加到 -lxxx 前面，  让没有使用的链接库也能链接上


